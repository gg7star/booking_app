<%= form_for(custome_booking) do |f| %>
  <% if custome_booking.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(custome_booking.errors.count, "error") %> prohibited this custome_booking from being saved:</h2>

      <ul>
      <% custome_booking.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    Customer Name<br/>
    <%= f.select :customer_id, @customers, prompt: "select a field" %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
